docker run -it fedora /bin/bash
(to pull the latest image from dockerhub cloud..the latest version and opens the bash in it for typing commands)
docker pull -a fedora
(pulls all the images of fedora from dockerhub cloud.)
docker ps (list all containers available)
docker attach containerID (attaches a container)
CTRL+P+Q quit to docker host.

docker ps
docker commit f9c44a915ee6 newimage
docker run newimage

docker history newimage
docker save -o /newimage.tar newimage
docker run --cpu-shares=256
docker run memory=1g
docker inspect <dockerid>
docker attach <dockerName>



